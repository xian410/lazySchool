<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.management.mapper.RequireTypeMapper">

    <select id="selectType" resultType="java.lang.String" parameterType="java.lang.String">
        select name from require_type where type_id = #{typeId}
    </select>

</mapper>
